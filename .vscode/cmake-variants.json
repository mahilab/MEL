{
  "buildType": {
    "default": "release",
    "description": "Build Type",
    "choices": {
      "debug": {
        "short": "Debug",
        "long": "Build with debugging information",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "long": "Optimize the resulting binaries",
        "buildType": "Release"
      }
    }
  },
  "static": {
    "default": "off",
    "choices": {
      "on": {
        "short": "Static",
        "long": "Build MEL as static library",
        "settings": {
          "MEL_STATIC": "ON"
        }
      },
      "off": {
        "short": "Dynamic",
        "long": "Build MEL as dynamic library",
        "settings": {
          "MEL_STATIC": "OFF"
        }
      }
    }
  },
  "quanser": {
    "default": "off",
    "choices": {
      "on": {
        "short": "Quanser",
        "long": "Build with Quanser DAQ support",
        "settings": {
          "QUANSER": "ON"
        }
      },
      "off": {
        "short": "No Quanser",
        "long": "Build without Quanser DAQ support",
        "settings": {
          "QUANSER": "OFF"
        }
      }
    }
  },
  "examples": {
    "default": "on",
    "choices": {
      "on": {
        "short": "Examples",
        "long": "Build examples",
        "settings": {
          "EXAMPLES": "ON"
        }
      },
      "off": {
        "short": "No Examples",
        "long": "Do not build examples",
        "settings": {
          "EXAMPLES": "OFF"
        }
      }
    }
  }
}