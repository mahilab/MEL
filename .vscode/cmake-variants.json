{
  "buildType": {
    "default": "release",
    "description": "Build Type",
    "choices": {
      "release": {
        "short": "Release",
        "long": "Optimize the resulting binaries",
        "buildType": "Release"
      },
      "debug": {
        "short": "Debug",
        "long": "Build with debugging information",
        "buildType": "Debug"
      }
    }
  },
  "shared": {
    "default": "static",
    "choices": {
      "static": {
        "short": "Static",
        "long": "Build as static library",
        "settings": {
          "MEL_SHARED": "OFF"
        }
      },
      "shared": {
        "short": "Shared",
        "long": "Build as shared library",
        "settings": {
          "MEL_SHARED": "ON"
        }
      }
    }
  },
  "examples": {
    "default": "on",
    "choices": {
      "on": {
        "short": "Examples",
        "long": "Build examples",
        "settings": {
          "MEL_EXAMPLES": "ON"
        }
      },
      "off": {
        "short": "No Examples",
        "long": "Do not build examples",
        "settings": {
          "MEL_EXAMPLES": "OFF"
        }
      }
    }
  },
  "quanser": {
    "default": "off",
    "choices": {
      "off": {
        "short": "No Quanser",
        "long": "Build without Quanser support",
        "settings": {
          "MEL_QUANSER": "OFF"
        }
      },
      "on": {
        "short": "Quanser",
        "long": "Build with Quanser support",
        "settings": {
          "MEL_QUANSER": "ON"
        }
      }
    }
  },
  "myo": {
    "default": "off",
    "choices": {
      "off": {
        "short": "No Myo",
        "long": "Build without Myo support",
        "settings": {
          "MEL_MYO": "OFF"
        }
      },
      "on": {
        "short": "Myo",
        "long": "Build with Myo support",
        "settings": {
          "MEL_MYO": "ON"
        }
      }
    }
  }
}